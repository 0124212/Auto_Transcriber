# Core — always installed
flask>=3.0
python-dotenv>=1.0

# Transcription engine (local Whisper)
openai-whisper>=20231117
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
ffmpeg-python>=0.2.0
pydub>=0.25.1
audioop-lts>=0.2.1

# Cloud providers (optional — only needed for Groq / OpenAI modes)
groq>=0.9.0
openai>=1.0.0
